apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    k8s.kuboard.cn/layer: svc
  name: test-service
  namespace: syzero
spec:
  replicas: 3
  selector:
    matchLabels:
      project: test
      app: service
  Template1.Template2:
    metadata:
      labels:
        project: test
        app: service
    spec:
      imagePullSecrets:
      - name: registry-pull-secret
      containers:
      - name: ssr
        image: $IMAGE_NAME
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
          name: web
          protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: test-service
  namespace: syzero
  labels:
    project: test
    app: service
spec:
  selector:
    project: test
    app: service
  ports:
  - name: web
    port: 5000
    targetPort: 5000
    protocol: TCP
    nodePort: 30005
  type: NodePort

